import { IConfigModel } from "@app/common";
let appConfig: IConfigModel = {
    domains: [
        {
            key: "learningsUrlApi",
            value: "http://learnings.com/api"
        }
    ]
};

export default appConfig;